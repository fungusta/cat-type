# Cat Type v1.0.39 release plan

**Goal:** Publish the approved automatic-settings-save change as the next stable release.

**Architecture:** Keep `IS_BETA_BUILD=False`, synchronize runtime and package
version metadata, and use the existing five-platform GitHub Release workflow.

## Preparation

- [x] Confirm the local changes are the approved automatic-save implementation,
  main matches origin, and v1.0.39 is unused.
- [x] Update runtime, macOS, and Windows metadata, version tests, and README to 1.0.39.
- [x] Add user-facing release notes at `docs/releases/v1.0.39.md`.
- [x] Run the release-version checker, full local suite, and diff checks.
- [x] Obtain a read-only release review; no blocking findings.
- [ ] Commit the verified candidate.

## Publication

- [ ] Push main without force and run the Release workflow on main as a dry run.
- [ ] Verify normal CI and all five release builds before tagging that exact commit.
- [ ] Create and push annotated v1.0.39; wait for all builds and publication.
- [ ] Set the release title, notes, and latest stable status.
- [ ] Verify tag target, release status, five packages and manifest, and downloaded checksums.
- [ ] Record release evidence, push documentation, and provide the release link.

Existing release tags and assets remain immutable. If a build fails, diagnose it
before publication; never move a published tag.
