# Cat Type v1.0.37 beta release

**Goal:** Publish the approved secret achievement items as a designated beta.

**Scope:** Include separate wardrobe/achievement tabs, nine new item rewards,
three equipment slots, secret discovery, Beta Buddy, and compatibility/performance
fixes. Keep all existing rewards and user settings. The user authorized release.

## Constraints

- New immutable tag `v1.0.37`; synchronize all four platform metadata files.
- Keep `IS_BETA_BUILD=True`; publish as a prerelease without replacing latest stable.
- Ship the Windows installer, signed/notarized Intel and ARM macOS DMGs, both Linux
  archives, and SHA256SUMS.txt using the existing five-platform release workflow.
- Release notes describe discovery without revealing secret requirements.

## Steps

- [x] Update version metadata and regression expectations; make the publish job
  read the checked-out beta designation before selecting GitHub release flags.
- [x] Run the local suite and version check; review the candidate changes.
- [x] Commit, integrate and push main without force; run the Release workflow
  without a tag and verify all five build/test/package jobs succeed.
- [x] Tag the verified commit, wait for publication and apply reviewed release notes.
- [x] Verify beta/latest status, tag target, six assets and downloaded checksums;
  record release evidence and return its public URL.

Published [Cat Type v1.0.37 Beta](https://github.com/fungusta/cat-type/releases/tag/v1.0.37).
See [release verification](../../research/2026-09-08-v1.0.37-release-verification.md)
for the tagged commit, successful workflow runs, and downloaded package checksums.
