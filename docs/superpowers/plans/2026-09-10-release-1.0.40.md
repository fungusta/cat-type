# Cat Type v1.0.40 release plan

**Goal:** Publish the approved expanded achievements, items, and cape-fit change
as the next stable release.

**Architecture:** Keep `IS_BETA_BUILD=False`, synchronize all runtime and package
version metadata, and use the existing five-platform GitHub Release workflow.

## Preparation

- [x] Confirm the release commit contains the approved catalog, SVG artwork,
  renderer layering, tests, and documentation.
- [x] Update runtime, macOS, Windows metadata, version tests, README, and release notes to 1.0.40.
- [x] Run the release-version checker, full local suite, focused artwork suite,
  and diff checks.
- [x] Commit the verified release candidate.

## Publication

- [x] Push the release commit to `main` without force.
- [x] Run regular CI and a five-package release dry run on the exact commit.
- [x] Create and push annotated `v1.0.40` from that exact commit.
- [x] Wait for all five package builds and GitHub Release publication.
- [x] Verify the tag target, release status, five packages, manifest, and checksums.
- [x] Record release evidence in `docs/research/2026-09-11-v1.0.40-release-verification.md`.

Existing release tags and assets remain immutable. If a build fails, diagnose it
before publication; never move a published tag.
